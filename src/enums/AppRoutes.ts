const AppRoutes = {
  HOME: "/",
  CATEGORIES: "/categories",
  SUBCATEGORIES: "/subcategories",
  ABOUT: "/about",
  PRODUCTS: "/products",
  SEARCH: "/search",
  REGISTER: "/register",
  TEST_CATEGORIES: "/test-categories", //zustand
} as const;

export default AppRoutes;
